# Session handoff — working memory

*Maintained by the `session-handoff` skill (.claude/skills/session-handoff).
Newest on top; keep last 3 sessions; git history keeps the rest.*

## 2026-06-11 — ECC cherry-pick install + security workspace plan
**Worked (verified):** ECC components installed project-scope into `.claude/`
(5 agents, 4 skills, 4 rules — reviewed before copy, no hooks runtime); all
repo gates pass (`tools/check_floor.py`, `tools/check_copy.py`).
**Failed / dead ends:** none this session. Note: user-level `~/.claude`
install not reachable from Cowork sandbox — run `./install.sh --profile
minimal --target claude` from a terminal if user-scope is ever wanted.
**Untried / next:** open logo-lab / crt-lab / tessera-concepts in a real
browser (no browser in sandbox — only code-verified); GitHub push (founder
item #2, top risk); NFC pilot order (HARDWARE.md §4); port chosen crt-lab
values into a `mint.css` for the studio focus view.
**Watch out:** light-grounded archetypes (polaroid/postcard) force their own
ink/ground for AA — don't "fix" that by feeding venue colors back in.
SVG-heavy pages: animations restart via clone-swap, not class toggling.
